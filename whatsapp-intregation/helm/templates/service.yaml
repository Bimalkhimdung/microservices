apiVersion: v1
kind: Service
metadata:
  name: {{ include "whatsapp-intregation.fullname" . }}
  labels:
    {{- include "whatsapp-intregation.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "whatsapp-intregation.selectorLabels" . | nindent 4 }}
